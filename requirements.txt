# osu!mania toolkit plugin dependencies.
# Pillow/aiohttp ship with AstrBot; the rest are needed by the analysis/render
# subsystems. AstrBot auto-installs this file when the plugin is loaded.
numpy>=1.26.0
scipy>=1.7.0
matplotlib>=3.5.0
pandas>=2.2.0,<3.0.0
onnxruntime>=1.23.2,<1.24.0
Pillow>=10.0.0
